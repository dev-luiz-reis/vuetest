# To run the project create a database called api and run the following commands:

docker compose up -d

docker-compose exec app bash

php artisan migrate

# inside the frontend folder run the command:

npm run serve
